
apiVersion: v1
kind: Service
metadata:
  name: voice-agent-server
  labels:
    app: voice-agent-server
spec:
  selector:
    app: voice-agent-server
  ports:
  - name: http
    port: 80
    targetPort: 8080
